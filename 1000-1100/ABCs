#include <iostream>
#include <vector>
#include <fstream>
#include <string.h>
#include <algorithm>
#include <bits/stdc++.h> 


using namespace std; 
 
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    long long arr[7]; 
    for(int i = 0; i<7; i++){
        cin>>arr[i]; 
    }
    long long n = sizeof(arr)/sizeof(arr[0]); 
    sort(array, array+n, greater<int>()); 
    cout<<arr[0]<<" "arr[1]<<" "<<arr[6]; 

    return 0;
} 
